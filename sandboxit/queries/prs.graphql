query {{
    {entity}(login: "{entity_login}") {{
        repositories(first: {repository}) {{
            nodes {{
                sshUrl
                name
                open: pullRequests(first: {pr}, states: OPEN) {{
                    nodes {{
                        number
                        branch: headRefName
                    }}
                }}
                merged: pullRequests(first: {pr}, states: MERGED) {{
                    nodes {{
                        number
                    }}
                }}
                closed: pullRequests(first: {pr}, states: CLOSED) {{
                    nodes {{
                        number
                    }}
                }}
            }}
        }}
    }}
}}
